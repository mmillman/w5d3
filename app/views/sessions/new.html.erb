<h1>Log In</h1>

<%#= form_tag(new_session_url, method: :post) do |f| %>
<%= form_for(:user, :url => session_path) do |f| %>

<%= f.label :email %>
<%= f.text_field :email %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.submit "Log in"%>

<% end %>